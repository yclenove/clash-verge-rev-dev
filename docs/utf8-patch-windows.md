# Windows UTF-8 ??????

## ??

? Windows ?? **PowerShell** ????????????????? ASCII??????

- PowerShell here-string?`@'...'@` / `@"..."@`?
- ??? UTF-8 ??? / ???
- ?????????? CP936???????

???????????????????

- ???? `?`
- ??/UI ??
- ???????????????????????

## ??

1. **?????????**??? **UTF-8 ?????**?`.py` / `.ts` / `.json` / `.md`????? PowerShell ????????
2. ? **Python 3**?`encoding="utf-8"`????????
3. **???anchor???? ASCII** ????????????? UTF-8 ???????
4. **??????????**?CRLF/LF???????????
5. `replace` ?? **??? 1 ?**?0 ????????????????

## ????

| ?? | ?? |
|------|------|
| `scripts/utf8_patch.py` | ? import / CLI ? UTF-8 ??????? |
| `scripts/templates/utf8_patch_task.template.py` | ????????? |

### CLI ??

```bash
# ???UTF-8?
python scripts/utf8_patch.py read src/locales/zh/home.json

# ????? old/new ??????old/new ?????? UTF-8?
python scripts/utf8_patch.py replace src-tauri/src/core/foo.rs --old-file old.txt --new-file new.txt

# ?????
python scripts/utf8_patch.py write src/utils/bar.ts --file bar.ts.utf8 --newline keep
```

### ??????????? Agent?

```bash
# 1. ????
cp scripts/templates/utf8_patch_task.template.py scripts/_task_my_edit.py

# 2. ? _task_my_edit.py ??? PATCHES / WRITES??????? py ????

# 3. ??
python scripts/_task_my_edit.py

# 4. ?????????????
rm scripts/_task_my_edit.py
```

### ??????

```python
from pathlib import Path
import sys
sys.path.insert(0, str(Path("scripts").resolve()))
from utf8_patch import replace_once

replace_once(
    "src-tauri/src/core/manager/state.rs",
    'logging!(info, Type::Core, "Starting core in sidecar mode");',
    'logging!(info, Type::Core, "??? sidecar ??????");',
    label="sidecar-start",
)
```

## Agent / Codex ??

??????? Windows???????????

1. **??**? PowerShell here-string ???????????
2. **??**? `scripts/utf8_patch.py` ??? `utf8_patch_task.template.py` ? UTF-8 Python ???
3. ????? ASCII anchor??????????????? `.py` ?????
4. ?? `git commit`??????????
5. ???? `dos2unix` / ??? EOL?

## ??

```bash
python -c "from pathlib import Path; t=Path('path/to/file').read_text(encoding='utf-8'); print('ok', '??????' in t)"
```

? `in` ? False ???????????????????????????????????
